#include <iostream>
#include <stack>
using namespace std;

int **mem;

int maxWeightIndependentSet(int i, int)

    int main()
{

    mem = new int *[n + 1];
    for (int i = 0; i < n + 1; i++)
    {
        mem[i] = new int[n + 1];
        for (int j = 0; j < n + 1; j++)
        {
            mem[i][j] = -1;
        }
    }
    cout << p(n, n - 1) << endl;
}
