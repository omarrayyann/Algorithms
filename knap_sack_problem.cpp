#include <iostream>
#include <vector>
#include <climits>

using namespace std;

int knapsack(vector<int> &values, vector<int> &weights, int weight)
{
    if (weight <)
}

int main()
{

    vector<int> values = {};
    vector<int> weights = {};
    int max_weight = 5;
}